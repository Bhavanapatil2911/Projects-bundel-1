import { FormatcurpipePipe } from './formatcurpipe.pipe';

describe('FormatcurpipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatcurpipePipe();
    expect(pipe).toBeTruthy();
  });
});
